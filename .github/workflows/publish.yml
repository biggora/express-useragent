name: Publish

on:
  push:
    tags:
      - v*

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm-publish

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          package-manager-cache: false

      - name: Verify tag matches package version
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          PKG_VERSION=$(node -p "require('./package.json').version")
          if [ "$TAG_VERSION" != "$PKG_VERSION" ]; then
            echo "::error::Tag ${GITHUB_REF_NAME} does not match package.json version ${PKG_VERSION}."
            echo "::error::Create a 'chore: release v${PKG_VERSION}' style commit that bumps package.json, then tag that commit."
            exit 1
          fi
          echo "Tag ${GITHUB_REF_NAME} matches package.json version ${PKG_VERSION}."

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test

      - name: Run high-severity audit
        run: npm audit --audit-level=high

      - name: Build package
        run: npm run build

      - name: Verify package contents
        run: |
          TAG_ARG=$(node -p "require('./package.json').version.includes('-') ? '--tag oidc-test' : ''")
          npm publish --dry-run $TAG_ARG

      - name: Publish package
        run: |
          TAG_ARG=$(node -p "require('./package.json').version.includes('-') ? '--tag oidc-test' : ''")
          npm publish $TAG_ARG

      # Fallback for token-based publishing if OIDC trusted publishing is unavailable.
      # Keep this disabled for the trusted publisher path; OIDC does not require NODE_AUTH_TOKEN.
      # - name: Publish package with npm token
      #   run: npm publish
      #   env:
      #     NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
